#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("KHUVUICHOI.inp","r",stdin);
    freopen("KHUVUICHOI.out","w",stdout);
    int a,b;
    cin>>a>>b;
    int m=b-a;
    int k=0,
    tong=0;
    for(int i=a+1;i<=b;i++){
        if((i<=12)&&(k<4)){
            tong+=6;
            k++;
        }
        else if((i>12)&&(k<4)){
            tong+=10;
            k++;
        }
        else if((i<=12)){
            tong+=3;
        }
        else{
            tong+=5;
        }
    }
    cout<<tong;
    return 0;
}
