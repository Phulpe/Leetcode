#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("demso.inp","r",stdin);
    freopen("demso.out","w",stdout);
    long long a,b,c,h;
    cin>>a>>b>>c;
    h=max(a/c*c,(a+c-1)/c*c);
    cout<<(b-a+1)-((b-h)/c+1);
    return 0;
}
